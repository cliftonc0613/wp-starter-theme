---
phase: 04-build-verification
plan: 01
type: execute
---

<objective>
Verify that the merged codebase builds successfully and tests pass.

Purpose: Ensure all PWA integration work from Phases 2-3 produces a working build with no regressions.
Output: Successful build and test run confirming merge integrity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conflict-resolution/02-01-SUMMARY.md
@.planning/phases/02-conflict-resolution/02-02-SUMMARY.md
@.planning/phases/03-file-integration/03-01-SUMMARY.md

**Prior phase context:**
- Phase 2: Resolved all conflicting files (package.json, next.config.ts, layout.tsx, package-lock.json)
- Phase 3: Added all new PWA files (offline page, manifest, icons, script)
- package-lock.json already regenerated in Phase 2

**Constraining decisions:**
- Use existing package-lock.json (already regenerated in Phase 2)
- Run standard npm commands for verification
- Fix any build/test errors before completing phase

**Build expectations:**
- Next.js 16.1.1 production build
- PWA service worker generation (via @ducanh2912/next-pwa)
- TypeScript compilation
- Vitest for unit tests
- Playwright for e2e tests (if configured)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production build</name>
  <files>frontend/.next/</files>
  <action>
    Run the Next.js production build to verify all code compiles correctly:

    ```bash
    cd frontend && npm run build
    ```

    This will:
    1. Compile TypeScript code
    2. Generate production bundles
    3. Create PWA service worker (via next-pwa)
    4. Run any build-time optimizations

    If build fails:
    - Read the error message carefully
    - Fix TypeScript/compilation errors in the affected files
    - Re-run build until successful

    Do NOT proceed until build succeeds.
  </action>
  <verify>
    - `npm run build` exits with code 0
    - No TypeScript errors in output
    - `.next/` directory contains build output
  </verify>
  <done>Production build completes without errors</done>
</task>

<task type="auto">
  <name>Task 2: Run tests</name>
  <files>None (verification only)</files>
  <action>
    Run the test suite to verify no regressions:

    ```bash
    cd frontend && npm run test:run
    ```

    This runs vitest in run mode (non-interactive).

    If tests fail:
    - Review failing test output
    - Determine if failure is related to PWA changes or pre-existing
    - Fix test issues if PWA-related
    - Note pre-existing failures in summary (don't fix unrelated issues)

    Note: e2e tests (Playwright) are not run in this phase - those require a running server.
  </action>
  <verify>
    - `npm run test:run` exits with code 0 OR reports only pre-existing failures
    - No new test failures introduced by PWA merge
  </verify>
  <done>Tests pass or only have pre-existing failures (document any in summary)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeded
- [ ] No TypeScript errors
- [ ] Tests pass or only pre-existing failures
- [ ] PWA service worker generated (check .next/ output)
</verification>

<success_criteria>

- Production build completes without errors
- Test suite passes (or only pre-existing failures documented)
- No regressions introduced by PWA merge
- Ready for Phase 5: PWA Verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-verification/04-01-SUMMARY.md`
</output>
