---
phase: 05-pwa-verification
plan: 01
type: execute
---

<objective>
Verify that PWA features work correctly in the production build.

Purpose: Confirm the merged PWA implementation functions as expected before completing the merge.
Output: Verified PWA functionality (service worker, manifest, offline fallback) and completed merge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-verification/04-01-SUMMARY.md

**Prior phase context:**
- Phase 4: Build passes, all 113 tests pass, skipWaiting in workboxOptions

**Constraining decisions:**
- PWA uses @ducanh2912/next-pwa v10.2.9
- Service worker generated via next-pwa workbox integration
- Offline fallback configured to /offline

**Key files:**
- frontend/next.config.ts (PWA configuration)
- frontend/public/manifest.json (PWA manifest)
- frontend/app/offline/page.tsx (offline fallback)

**Verification targets:**
1. Service worker registers in production build
2. Manifest loads at /manifest.json
3. Offline fallback works when disconnected
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start production server and verify manifest</name>
  <files>None (verification only)</files>
  <action>
    Start the Next.js production server and verify the PWA manifest is accessible:

    ```bash
    cd frontend && npm run start &
    sleep 5
    curl -s http://localhost:3000/manifest.json | head -20
    ```

    Verify:
    1. Server starts without errors
    2. manifest.json returns valid JSON
    3. manifest.json contains correct PWA properties (name, icons, start_url)

    Note: The server will run in the background for subsequent verification.
  </action>
  <verify>
    - Server starts on port 3000
    - curl http://localhost:3000/manifest.json returns JSON with "name": "Starter WP Theme"
    - Response includes icons array with multiple sizes
  </verify>
  <done>Production server running, manifest.json accessible and valid</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Production PWA build with service worker and offline support</what-built>
  <how-to-verify>
    1. Open browser: http://localhost:3000
    2. Open DevTools (F12 or Cmd+Opt+I)
    3. Go to Application tab → Service Workers
    4. Confirm: Service worker is registered and activated
    5. Go to Application tab → Manifest
    6. Confirm: Manifest shows "Starter WP Theme" with icons
    7. Test offline:
       a. In DevTools Network tab, check "Offline" checkbox
       b. Navigate to any page or refresh
       c. Confirm: Offline page displays with "You're Offline" message
       d. Uncheck "Offline" to restore connection
    8. Confirm: All PWA features work as expected
  </how-to-verify>
  <resume-signal>Type "approved" to complete PWA verification, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Production server starts without errors
- [ ] manifest.json is accessible at /manifest.json
- [ ] Service worker registers (human verified)
- [ ] Offline fallback displays (human verified)
- [ ] Kill background server process after verification
</verification>

<success_criteria>

- Production build serves PWA manifest correctly
- Service worker registers in browser
- Offline page displays when network is disabled
- Phase 5 complete, merge ready for final commit
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa-verification/05-01-SUMMARY.md`

Kill background server:
```bash
pkill -f "next start" || true
```

Update ROADMAP.md to mark Phase 5 complete.
Update STATE.md to reflect merge completion.
</output>
