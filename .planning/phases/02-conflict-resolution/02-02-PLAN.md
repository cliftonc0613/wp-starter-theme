---
phase: 02-conflict-resolution
plan: 02
type: execute
---

<objective>
Resolve layout.tsx merge conflict and regenerate package-lock.json.

Purpose: Add PWA metadata to develop's layout structure and create clean lock file with all dependencies.
Output: Working layout.tsx with PWA metadata and fresh package-lock.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conflict-resolution/02-01-SUMMARY.md

**Prior plan context:**
- package.json updated with PWA deps
- next.config.ts updated with PWA wrapper chain

**Constraining decisions:**
- Keep ALL develop's layout structure (Providers, StructuredData, fonts, etc.)
- ADD PWA metadata (viewport, manifest, icons, apple-web-app tags)
- Delete and regenerate package-lock.json for clean resolution

**Files to modify:**
- `frontend/app/layout.tsx` - Add PWA metadata
- `frontend/package-lock.json` - Delete and regenerate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update layout.tsx with PWA metadata</name>
  <files>frontend/app/layout.tsx</files>
  <action>
    Update layout.tsx to add PWA support:

    1. Update the import to include Viewport:
       ```typescript
       import type { Metadata, Viewport } from "next";
       ```

    2. Add viewport export after the organizationSchema definition:
       ```typescript
       export const viewport: Viewport = {
         themeColor: "#0a0a0a",
         width: "device-width",
         initialScale: 1,
         maximumScale: 5,
         userScalable: true,
       };
       ```

    3. Add PWA properties to the metadata export (after robots):
       ```typescript
       manifest: "/manifest.json",
       appleWebApp: {
         capable: true,
         statusBarStyle: "default",
         title: "Starter WP Theme",
       },
       formatDetection: { telephone: false },
       icons: {
         icon: [
           { url: "/icons/icon-192x192.png", sizes: "192x192", type: "image/png" },
           { url: "/icons/icon-512x512.png", sizes: "512x512", type: "image/png" },
         ],
         apple: [
           { url: "/icons/icon-152x152.png", sizes: "152x152", type: "image/png" },
           { url: "/icons/icon-192x192.png", sizes: "192x192", type: "image/png" },
         ],
       },
       ```

    4. Add PWA head tags inside the html element, before body:
       ```tsx
       <head>
         <link rel="apple-touch-icon" href="/icons/icon-192x192.png" />
         <meta name="apple-mobile-web-app-capable" content="yes" />
         <meta name="apple-mobile-web-app-status-bar-style" content="default" />
         <meta name="apple-mobile-web-app-title" content="StarterWP" />
         <meta name="mobile-web-app-capable" content="yes" />
         <meta name="msapplication-TileColor" content="#0a0a0a" />
         <meta name="msapplication-tap-highlight" content="no" />
       </head>
       ```

    Keep all existing develop content (fonts, Providers wrapper, StructuredData, Header, Footer, etc.)
  </action>
  <verify>
    Read layout.tsx and confirm:
    - Import includes Viewport
    - viewport export defined
    - metadata includes manifest, appleWebApp, formatDetection, icons
    - head element with PWA meta tags present
    - All original develop content preserved (fonts, Providers, StructuredData, etc.)
  </verify>
  <done>layout.tsx has full PWA metadata while preserving all develop functionality</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate package-lock.json</name>
  <files>frontend/package-lock.json</files>
  <action>
    1. Delete existing package-lock.json:
       rm frontend/package-lock.json

    2. Run npm install to regenerate with new dependencies:
       cd frontend && npm install

    This will create a fresh lock file with:
    - All original develop dependencies
    - New PWA dependencies (@ducanh2912/next-pwa, sharp)
    - Proper dependency tree resolution
  </action>
  <verify>
    - package-lock.json exists
    - package-lock.json includes @ducanh2912/next-pwa
    - package-lock.json includes sharp
    - npm install completed without errors
  </verify>
  <done>Fresh package-lock.json generated with all dependencies resolved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] layout.tsx imports Viewport type
- [ ] layout.tsx exports viewport config
- [ ] layout.tsx has PWA metadata (manifest, icons, appleWebApp)
- [ ] layout.tsx has PWA head tags
- [ ] package-lock.json regenerated
- [ ] npm install succeeded
- [ ] All develop functionality preserved
</verification>

<success_criteria>

- layout.tsx fully updated with PWA metadata
- package-lock.json regenerated with all dependencies
- No npm install errors
- Phase 2 complete, ready for Phase 3 (File Integration)
</success_criteria>

<output>
After completion, create `.planning/phases/02-conflict-resolution/02-02-SUMMARY.md`
</output>
