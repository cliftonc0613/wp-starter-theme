---
phase: 02-conflict-resolution
plan: 01
type: execute
---

<objective>
Resolve package.json and next.config.ts merge conflicts.

Purpose: Combine PWA dependencies and Next.js config wrappers with develop's existing Sentry and testing setup.
Output: Working package.json with PWA deps and next.config.ts with chained wrappers (PWA → BundleAnalyzer → Sentry).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-branch-preparation/01-01-SUMMARY.md

**Prior phase context:**
- On branch: `merge/pwa-into-develop`
- Based on: develop (commit 286cbe3)
- PWA source: `claude/wordpress-pwa-compatibility-yqSbG`

**Constraining decisions:**
- Chain config wrappers: PWA → BundleAnalyzer → Sentry in next.config.ts
- Keep ALL develop dependencies and scripts
- ADD PWA deps: @ducanh2912/next-pwa, sharp (devDep)
- ADD PWA script: generate-icons

**Files to modify:**
- `frontend/package.json` - Add PWA deps and script
- `frontend/next.config.ts` - Add PWA wrapper to chain
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json with PWA dependencies</name>
  <files>frontend/package.json</files>
  <action>
    Add the following to package.json:

    1. In "scripts" object, add:
       "generate-icons": "node scripts/generate-icons.mjs"

    2. In "dependencies" object, add:
       "@ducanh2912/next-pwa": "^10.2.9"

    3. In "devDependencies" object, add:
       "sharp": "^0.34.5"

    Keep all existing scripts, dependencies, and devDependencies from develop.
    Do NOT change the version number (keep "1.2.1").
  </action>
  <verify>
    Read package.json and confirm:
    - scripts includes "generate-icons"
    - dependencies includes "@ducanh2912/next-pwa"
    - devDependencies includes "sharp"
    - All original develop deps still present (@sentry/nextjs, vitest, @playwright/test, etc.)
  </verify>
  <done>package.json has all develop deps plus PWA deps and generate-icons script</done>
</task>

<task type="auto">
  <name>Task 2: Update next.config.ts with PWA wrapper</name>
  <files>frontend/next.config.ts</files>
  <action>
    Transform next.config.ts to chain all three wrappers:

    1. Add import at top:
       import withPWAInit from "@ducanh2912/next-pwa";

    2. After the bundleAnalyzer setup, add the PWA initialization:
       ```typescript
       const withPWA = withPWAInit({
         dest: "public",
         disable: isDev,
         register: true,
         skipWaiting: true,
         reloadOnOnline: true,
         cacheOnFrontEndNav: true,
         aggressiveFrontEndNavCaching: true,
         fallbacks: {
           document: "/offline",
         },
         workboxOptions: {
           disableDevLogs: true,
           runtimeCaching: [
             {
               urlPattern: /^https:\/\/fonts\.(?:gstatic|googleapis)\.com\/.*/i,
               handler: "CacheFirst",
               options: {
                 cacheName: "google-fonts",
                 expiration: {
                   maxEntries: 10,
                   maxAgeSeconds: 365 * 24 * 60 * 60,
                 },
               },
             },
             {
               urlPattern: /\.(?:eot|otf|ttc|ttf|woff|woff2|font\.css)$/i,
               handler: "StaleWhileRevalidate",
               options: {
                 cacheName: "static-fonts",
                 expiration: {
                   maxEntries: 10,
                   maxAgeSeconds: 365 * 24 * 60 * 60,
                 },
               },
             },
             {
               urlPattern: /\.(?:jpg|jpeg|gif|png|svg|ico|webp)$/i,
               handler: "StaleWhileRevalidate",
               options: {
                 cacheName: "static-images",
                 expiration: {
                   maxEntries: 64,
                   maxAgeSeconds: 30 * 24 * 60 * 60,
                 },
               },
             },
             {
               urlPattern: /\.(?:js|css)$/i,
               handler: "StaleWhileRevalidate",
               options: {
                 cacheName: "static-assets",
                 expiration: {
                   maxEntries: 32,
                   maxAgeSeconds: 24 * 60 * 60,
                 },
               },
             },
             {
               urlPattern: /^https:\/\/.*\.(?:wpstarter\.mysites\.io|websiteplayground\.local)\/wp-json\/.*/i,
               handler: "NetworkFirst",
               options: {
                 cacheName: "wordpress-api",
                 expiration: {
                   maxEntries: 32,
                   maxAgeSeconds: 60 * 60,
                 },
                 networkTimeoutSeconds: 10,
               },
             },
             {
               urlPattern: /^https:\/\/.*\/wp-content\/uploads\/.*/i,
               handler: "CacheFirst",
               options: {
                 cacheName: "wordpress-uploads",
                 expiration: {
                   maxEntries: 64,
                   maxAgeSeconds: 30 * 24 * 60 * 60,
                 },
               },
             },
           ],
         },
       });
       ```

    3. Update the Sentry config to chain all three wrappers:
       Change from: `withSentryConfig(withBundleAnalyzer(nextConfig), {...})`
       To: `withSentryConfig(withBundleAnalyzer(withPWA(nextConfig)), {...})`

    Keep all existing develop config (images, env, Sentry options).
  </action>
  <verify>
    Read next.config.ts and confirm:
    - Import for withPWAInit present
    - withPWA config defined with all 6 caching strategies
    - Export chains: withSentryConfig(withBundleAnalyzer(withPWA(nextConfig)))
    - Original develop config preserved (images, env, Sentry options)
  </verify>
  <done>next.config.ts chains PWA → BundleAnalyzer → Sentry with all caching strategies</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] package.json has PWA deps (@ducanh2912/next-pwa, sharp)
- [ ] package.json has generate-icons script
- [ ] next.config.ts imports withPWAInit
- [ ] next.config.ts has withPWA config with 6 caching strategies
- [ ] next.config.ts exports chained config (PWA → BundleAnalyzer → Sentry)
- [ ] All original develop config preserved
</verification>

<success_criteria>

- package.json updated with PWA deps and script
- next.config.ts updated with PWA wrapper in chain
- All develop functionality preserved (Sentry, testing, bundle analyzer)
- Ready for Plan 02-02 (layout.tsx and package-lock regeneration)
</success_criteria>

<output>
After completion, create `.planning/phases/02-conflict-resolution/02-01-SUMMARY.md`
</output>
