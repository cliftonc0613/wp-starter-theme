---
phase: 03-file-integration
plan: 01
type: execute
---

<objective>
Add all new PWA files from the feature branch to the merge workspace.

Purpose: Integrate the PWA assets (offline page, manifest, icons, generation script) that don't exist in develop branch.
Output: Complete PWA file set ready for build verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conflict-resolution/02-01-SUMMARY.md
@.planning/phases/02-conflict-resolution/02-02-SUMMARY.md

**Prior phase context:**
- Phase 2 resolved all conflicting files
- package.json has PWA deps and generate-icons script
- next.config.ts has PWA wrapper chain
- layout.tsx has PWA metadata
- package-lock.json regenerated with all deps

**Constraining decisions:**
- Copy files from `claude/wordpress-pwa-compatibility-yqSbG` branch
- Preserve exact file contents (no modifications needed)

**Files to add:**
- `frontend/app/offline/page.tsx` - Offline fallback page
- `frontend/public/manifest.json` - PWA manifest
- `frontend/public/icons/*` - 10 PWA icon files
- `frontend/scripts/generate-icons.mjs` - Icon generation script
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PWA page and manifest files</name>
  <files>frontend/app/offline/page.tsx, frontend/public/manifest.json</files>
  <action>
    Copy these files from the PWA branch to the merge workspace:

    1. Create offline page directory and copy file:
       ```bash
       mkdir -p frontend/app/offline
       git show claude/wordpress-pwa-compatibility-yqSbG:frontend/app/offline/page.tsx > frontend/app/offline/page.tsx
       ```

    2. Copy manifest.json:
       ```bash
       git show claude/wordpress-pwa-compatibility-yqSbG:frontend/public/manifest.json > frontend/public/manifest.json
       ```

    Do NOT modify the file contents - copy exactly as they exist in the PWA branch.
  </action>
  <verify>
    - `cat frontend/app/offline/page.tsx | head -20` shows React component with "use client"
    - `cat frontend/public/manifest.json | head -10` shows "name": "Starter WP Theme"
    - Files exist and are not empty
  </verify>
  <done>offline/page.tsx and manifest.json exist with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Add PWA icons and generation script</name>
  <files>frontend/public/icons/*, frontend/scripts/generate-icons.mjs</files>
  <action>
    Copy icon files and generation script from PWA branch:

    1. Create icons directory and copy all icon files:
       ```bash
       mkdir -p frontend/public/icons
       cd frontend/public/icons
       for icon in icon-72x72.png icon-96x96.png icon-128x128.png icon-144x144.png icon-152x152.png icon-192x192.png icon-384x384.png icon-512x512.png icon-maskable-192x192.png icon-maskable-512x512.png; do
         git show claude/wordpress-pwa-compatibility-yqSbG:frontend/public/icons/$icon > $icon
       done
       ```

    2. Create scripts directory and copy generation script:
       ```bash
       mkdir -p frontend/scripts
       git show claude/wordpress-pwa-compatibility-yqSbG:frontend/scripts/generate-icons.mjs > frontend/scripts/generate-icons.mjs
       ```

    Copy all 10 icon files (8 standard + 2 maskable) exactly as they exist.
  </action>
  <verify>
    - `ls -la frontend/public/icons/*.png | wc -l` returns 10
    - `ls -la frontend/scripts/generate-icons.mjs` shows file exists
    - Icon files are valid PNGs (not empty)
  </verify>
  <done>All 10 icon files and generate-icons.mjs exist in correct locations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] frontend/app/offline/page.tsx exists
- [ ] frontend/public/manifest.json exists
- [ ] frontend/public/icons/ contains 10 PNG files
- [ ] frontend/scripts/generate-icons.mjs exists
- [ ] All files are non-empty and properly formatted
</verification>

<success_criteria>

- All PWA files copied from feature branch
- File structure matches roadmap specification
- No file modifications needed (exact copies)
- Phase 3 complete, ready for Phase 4 (Build Verification)
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-integration/03-01-SUMMARY.md`
</output>
