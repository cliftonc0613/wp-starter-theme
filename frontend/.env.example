# ===========================================
# Environment Variables for Headless WordPress
# ===========================================
# Copy this file to .env.local and update values
#
# DEVELOPMENT: Use your Local by Flywheel URL
# PRODUCTION: Use your production WordPress URL
# ===========================================

# WordPress API Configuration
# Replace with your WordPress site URL
# Development: http://your-site.local/wp-json/wp/v2
# Production: https://your-domain.com/wp-json/wp/v2
WORDPRESS_API_URL=http://your-wordpress-site.local/wp-json/wp/v2

# Preview Configuration
# Change this to a secure random string in production
# Generate with: openssl rand -base64 32
PREVIEW_SECRET=preview-secret-change-me

# Revalidation Configuration
# Change this to a secure random string in production
# Must match STARTER_REVALIDATION_SECRET in WordPress wp-config.php
# Generate with: openssl rand -base64 32
REVALIDATION_SECRET=revalidation-secret-change-me

# Site Configuration
# Your frontend URL (for Open Graph, canonical URLs, sitemap, etc.)
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Site Name (used in structured data and metadata)
NEXT_PUBLIC_SITE_NAME=Your Site Name

# ===========================================
# VERCEL DEPLOYMENT NOTES
# ===========================================
# Add these environment variables in Vercel Dashboard:
# - WORDPRESS_API_URL (production WordPress URL)
# - PREVIEW_SECRET (same as WordPress config)
# - REVALIDATION_SECRET (same as WordPress config)
# - NEXT_PUBLIC_SITE_URL (your Vercel deployment URL)
# - NEXT_PUBLIC_SITE_NAME (your site name)
# ===========================================
